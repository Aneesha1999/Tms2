@model IEnumerable<Tms2.Models.allocatevehicle>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employeelocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Empphoneno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employeeage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vehicleallocationtoEmployee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employeelocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empphoneno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employeeage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vehicleallocationtoEmployee)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Employeename">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Employeename">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Employeename">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
